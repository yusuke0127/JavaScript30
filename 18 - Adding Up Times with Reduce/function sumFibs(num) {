function sumFibs(num) {
  let a = 1, b = 0, temp, result = 0;
  while(b <= num) {
    temp = a;
    a = a + b;
    if (b % 2 !== 0){
      result += b
    }
    b = temp;
  }
  return result
}

sumFibs(10);
